// config.js — copy this file to config.js and customise it.
// Load it BEFORE app.js in index.html:
//   <script src="config.js"></script>
//   <script src="app.js"></script>
//
// Set PROXY_URL to the URL of a running proxy.js instance so that users
// do not need to supply their own Anthropic API key.
// Leave it as an empty string (or delete the line) to require users to enter a key.

window.PROXY_URL = "https://upgraded-fishstick.bertmaher.workers.dev"; // e.g. "https://your-proxy.example.com"
